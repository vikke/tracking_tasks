<%= form_with(model: task_master) do |form| %>
  <% if task_master.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(task_master.errors.count, "error") %> prohibited this task_master from being saved:</h2>

      <ul>
        <% task_master.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :task_name, style: "display: block" %>
    <%= form.text_field :task_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
